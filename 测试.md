# 我的世界材质包卡片
# [中文](./README.zh-CN.md)/[English](./README.en-US.md)
---

你还在用这种看起来老式的文本描述吗？
![演示图片](Mockup/01.png)  
不如试试这种卡片式的！你可以把它做成风景图，也可以做成一张名片。
![演示图片](Mockup/03.png)  
![演示图片](Mockup/02.png)  
---

⚠️ 临时注意事项

教程还没写完，所以先临时写一些注意事项。这些内容以后随时可能改动。

---

### 1
卡片没显示？
如果你发现卡片变成了这样：
![演示图片](Mockup/04.jpg)  
这是正常的！ 因为材质包还没被装载。装载之后就会正常显示卡片了。

如果你有办法让它在未装载时也能显示卡片，欢迎提交 PR，或者发邮件联系我。

---

### 2
你需要改哪些东西？
打开材质包信息文件：

```json
{
  "pack":{
    "pack_format":15,
    "supported_formats":[1, 64],
    "min_format":1,
    "max_format":147,
    "description":"第一行文本测试\n一二三四五六七八九十一二三四§f\ue145\ue146\ue147\ue148"
  }
}
```

其中：

·`\n`和`§f\ue145\ue146\ue147\ue148`→ 必须改，改法看[第 4 节](#4)  
· 第一行文本测试 → 可以改成你想显示的介绍文字，但要遵守[第 3 节](#3)的规则  
·`一二三四五六七八九十一二三四`→ 改法看[第 8 节](#8)

其他内容随便改。

---

### 3
介绍文字千万别超出第一行！

不要写成这样（超过第一行跑到第二行）：

![演示图片](Mockup/05.jpg)  

否则会显示错误：

![演示图片](Mockup/06.jpg)  

正确做法：

· 第一行写满了 → 用`\n`换行
· 第一行没写满 → 也要用`\n`换行

不过一般来说，第 2 节里已经让你保留`\n`了。

---

### 4
字符替换（重要！）

打开字符替换文件：

```json
{
 "providers":
  [
   {
   "type":"bitmap",
   "file":"minecraft:147/146.png",
   "ascent":28,
   "height":32,
   "chars": ["\ue146"]},
   {
   "type":"bitmap",
   "file":"minecraft:147/148.png",
   "ascent":28,
   "height":32,
   "chars": ["\ue148"]},
   {
   "type": "space",
   "advances":{"\ue147": -1.17}},
   {
   "type": "space",
   "advances":{"\ue145": -126}}
  ]
}
```

其中 \ue146 和 \ue148 必须改，改动规则如下（不遵守会出错，甚至无法装载）：

1. 不能继续用`\ue146`和`\ue148`  
2. 只能改`\ue`后面的数字部分，必须是三位数（不能多也不能少）
3. 如果你有其他材质包，不要和其他材质包用同样的名字，否则同时装载会冲突
4. 只能用这些字符：0-1、a-f、A-F
   · 可以纯数字：\ue198
   · 可以纯字母：\ueabc、\ueAbC
   · 可以字母加数字：\uea6f
5. \ue146 和 \ue148 改完后不能相同（比如都改成 \ue777 是不行的）
6. 改完后，材质包信息文件里的`\ue146`和`\ue148` 也要同步改成一样的

---

### 5
卡片图片尺寸  
· 146.png：长 256 × 宽 72  
· 148.png：长 80 × 宽 72  
---  
### 6
像素不能多也不能少！

多了少了都会影响图片显示。

---

### 7
两张图的关系
它们合起来是一张 256 + 80 × 72 的图片，你可以按照这个尺寸来制作你的卡片。

---

### 8
第二行文字后面的方块怎么对齐？

第二行介绍文字后面有四个方块，要放在这一行的最后面。

假设你把第二行改成 第二条文字介绍：

Mockup/07.jpg

可以看到第二行文字没占满整行，我们需要在它后面加空格，把方块推到最右边。但加多少空格呢？可以这样算：

计算表格：

字符类型 占用单位
初始数 126
汉字 9
字母 6
- 6
_ 6
全角空格 　 9
普通空格   4
§l 加粗后的汉字 9.5
§l 加粗后的字母 7

举例： 第二行有 7 个汉字

1. 先算剩余单位：126 − 9 × 7 = 63
2. 再算需要多少空格：63 ÷ 9 = 7
3. 所以需要在第二行文字后面、方块前面加 7 个全角空格

这样就能得到正确效果：

Mockup/08.jpg

注意事项：

1. 不能连续打 23 个空格，否则会被我的世界判定为「无效输入」
2. 空格不能多也不能少，方块位置不对会导致显示错误（卡片位置偏前、卡片不显示等）
   Mockup/09.jpg
   Mockup/10.jpg
3. 可以用表格以外的符号，但我不能保证它对应表格上的数字，也就没法算出正确的空格数
4. 所以建议不要用斜体等像 §l 那样改变字体的 § 编码（但改变颜色的 § 编码可以用，比如 §b）。表格以外的字符不建议使用。

---

很抱歉项目才刚开始，还没完善 -_-ll

（可以先来问候作者，让他先教你 😋）